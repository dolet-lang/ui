# UI Module Metadata
# Pure software UI + 2D drawing library for Dolet.

[info]
name = ui
version = 0.4.0
description = Native UI & drawing library for Dolet — pure software framebuffer canvas, bitmap text, draw command recording, mouse input

[dependencies]
window
fonts

[link.windows]
system_libs = user32, gdi32

[link.linux]
# Planned later.

[link.baremetal]
# No system libs needed.
