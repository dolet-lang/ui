# UI Module Metadata
# Pure software rendering via draw framebuffer.

[info]
name = ui
version = 0.2.0
description = Native UI drawing library — pure software renderer via draw

[dependencies]
window = optional
fonts

[link.windows]
system_libs = user32, gdi32

[link.linux]
# Planned later.

[link.baremetal]
# No system libs needed.
