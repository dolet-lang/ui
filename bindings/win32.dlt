# ============================
#  Win32 Input Bindings
# ============================

extern lib "user32":
    fun GetCursorPos(lpPoint: i64) -> i32
    fun ScreenToClient(hWnd: i64, lpPoint: i64) -> i32
    fun GetAsyncKeyState(vKey: i32) -> i16
